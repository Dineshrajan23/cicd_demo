version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: 'http://16.171.77.219:8001'
        NEXT_PUBLIC_WS_URL: 'ws://16.171.77.219:8001'
        NEXT_PUBLIC_CHAT_BACKEND_URL: 'http://16.171.77.219:8001'
        NEXT_PUBLIC_API_BASE: 'http://16.171.77.219:8001/api/v1/'
        NEXT_PUBLIC_BACKEND_URL: 'http://16.171.77.219'
    container_name: gameboss-frontend
    ports:
      - '3001:3000'
    environment:
      NODE_ENV: production
    restart: always